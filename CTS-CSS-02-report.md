# Отчет о проверке проекта на соответствие критерию CTS-CSS-02

## Критерий: Глобальный селектор

**Дата проверки:** 2024

## Описание критерия

Глобальные селекторы запрещены, кроме исключений:
1. Универсальный селектор `*` для изменения боксовой модели одновременно с псевдоэлементами
2. Нормализация тега `<img>` (только `max-width`, `height`, `object-fit`)
3. Уникальные теги документа: `<html>` и `<body>`
4. `:root` только с кастомными свойствами

---

## Результаты проверки

### ✅ Соответствует критерию

#### 1. Универсальный селектор для боксовой модели
**Файл:** `scss/base/null.scss` (строки 1-5)
```scss
*,
*::before,
*::after {
  box-sizing: border-box;
}
```
**Статус:** ✅ Правильно - используется для изменения боксовой модели с псевдоэлементами

#### 2. Уникальные теги `<html>` и `<body>`
**Файл:** `scss/base/null.scss` (строки 7-32)
```scss
body {
  margin: 0;
  padding: 0;
  scroll-behavior: smooth;
}

html,
body {
  min-width: $mobile-min-width;
  height: 100%;
  background-color: $mainBackground;
  font-size: calc($fz-default / $vp-1920 * 100vw);
  // ...
}

body {
  font-family: $fontFamily;
  line-height: 1;
  color: $mainColor;
  // ...
}
```
**Статус:** ✅ Правильно - уникальные теги документа разрешены

**Файл:** `scss/style.scss` (строки 19-44)
```scss
body {
  &::before {
    content: "";
    position: fixed;
    inset: 0;
    background-color: rgba(39, 38, 38, 0.5);
    // ...
  }
}

body.lock {
  overflow: hidden;
  // ...
}
```
**Статус:** ✅ Правильно - уникальный тег `<body>` разрешен

#### 3. `:root` с кастомными свойствами
**Файл:** `scss/base/custom-properties.scss` (строки 1-6)
```scss
:root {
  --z-header: 100;
  --mobile-padding: calc(15px / 16px * 1rem);
  --tablet-padding: calc(20px / 16px * 1rem);
  --desktop-padding: calc(30px / 16px * 1rem);
}
```
**Статус:** ✅ Правильно - используется только для кастомных свойств

#### 4. Вложенные селекторы по типу
Все найденные селекторы по типу вложены внутрь селекторов по классу:
- `scss/clients.scss` - `.clients__item img` (строка 149)
- `scss/portfolio.scss` - `.portfolio__description br` (строки 184, 198)
- `scss/footer.scss` - `.footer__title br` (строка 503)
- `scss/hero.scss` - `.hero__socials-icon-wrapper--w svg` и другие (строки 91-114)
- `scss/header.scss` - `.header__logo img` (строка 46)
- `scss/banner.scss` - `.banner__description br` (строки 665, 680)

**Статус:** ✅ Правильно - селекторы по типу вложены внутрь селекторов по классу

---

### ❌ Нарушения критерия

**Нарушений не найдено!** ✅

Все глобальные селекторы в файле `scss/base/null.scss` соответствуют исключениям критерия CTS-CSS-02.

---

## Статистика

- **Всего проверено файлов:** 18
- **Найдено нарушений:** 0 ✅
- **Соответствует критерию:** Все глобальные селекторы соответствуют исключениям

## Рекомендации по исправлению

**Исправления не требуются!** ✅

Все нарушения были исправлены:
- ✅ Глобальный селектор `<a>` удален, стили добавлены в соответствующие классы
- ✅ Глобальные селекторы `<input>`, `<button>`, `<textarea>` удалены
- ✅ Глобальный селектор `<img>` удален

---

## Заключение

Проект **полностью соответствует** критерию CTS-CSS-02. ✅

Все нарушения были исправлены:
- ✅ Глобальный селектор `<a>` удален из `null.scss`, стили `color: inherit;` и `text-decoration: none;` добавлены в соответствующие классы ссылок
- ✅ Глобальные селекторы `<input>`, `<button>`, `<textarea>` удалены
- ✅ Глобальный селектор `<img>` удален

**Текущее состояние файла `scss/base/null.scss`:**
- ✅ Универсальный селектор `*` с псевдоэлементами для `box-sizing` - соответствует исключению
- ✅ Уникальные теги `<html>` и `<body>` - соответствуют исключению
- ✅ Все остальные глобальные селекторы удалены

Все глобальные селекторы в проекте соответствуют исключениям критерия или являются вложенными селекторами, что разрешено.
